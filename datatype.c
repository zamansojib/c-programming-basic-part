#include <stdio.h>
int main(){
    
    // int num = 25;
    // float num2 =29.365f;
    // char ch = 'S';
    // printf("first answer is = %d\n",num);
    // printf("second answer is = %f\n",num2);
    // printf("third answer is = %c\n",ch);
    
    // num = num2;
    // printf("%d\n",num);

    // Team Alpha won the toss and choose to bat
    // int zaman = 5;
    // int bodor = 8;
    // int azad = 6;
    // int munna = 10;
    // int billah = 27;
    // int arif = 15;
    // int balls = 6;

    // int zaman_sr;
    // zaman_sr = (100*zaman)/6;
    // float zaman_sr1 = zaman_sr;

    // int bodor_sr;
    // bodor_sr = (100*bodor)/6;
    // float bodor_sr1 = bodor_sr;

    // int azad_sr;
    // azad_sr = (100*azad)/6;
    // float azad_sr1 = azad_sr;

    // int munna_sr;
    // munna_sr = (100*munna)/6;
    // float munna_sr1 = munna_sr;

    // int billah_sr;
    // billah_sr = (100*billah)/6;
    // float billah_sr1 = billah_sr;

    // int arif_sr;
    // arif_sr = (100*arif)/6;
    // float arif_sr1 = arif_sr;

    
    // int total_ball = balls*6;
    // int extra_run = 1+3+4+2;
    // int total_run = (zaman+bodor+azad+munna+billah+arif)+extra_run;
     
    //  printf("\nScorecard Team Alpha:\n");
    // printf("1.Zaman = %d\tBalls: %d\tSR: %f\n",zaman,balls,zaman_sr1); 
    // printf("2.Bodor = %d\tBalls: %d\tSR: %f\n",bodor,balls,bodor_sr1); 
    // printf("3.Azad = %d\tBalls: %d\tSR: %f\n",azad,balls,azad_sr1); 
    // printf("4.Munna = %d\tBalls: %d\tSR: %f\n",munna,balls,munna_sr1); 
    // printf("5.Billah = %d\tBalls: %d\tSR: %f\n",billah,balls,billah_sr1); 
    // printf("6.Arif = %d\tBalls: %d\tSR: %f\n",arif,balls,arif_sr1);
    // printf("Total Run = %d\tTotal Ball = %d\n",total_run,total_ball);
    // printf("Extra Run = %d\n\n",extra_run);

    // Team Beta which Bowl First
    // int alif = 6;
    // int pranjal = 5;
    // int shahil = 10;
    // int torongo = 15;
    // int debashis = 20;
    // int rakib = 16;

    // int over = 1;
    // int total_over = over*6;
    // int economy_rate1 = alif/6;
    // int economy_rate2 = pranjal/6;
    // int economy_rate3 = shahil/6;
    // int economy_rate4 = torongo/6;
    // int economy_rate5 = debashis/6;
    // int economy_rate6 = rakib/6;
    

    // printf("\nScorecatrd Team Beta:\n");
    // printf("Alif give run = %d\tOver = %d\tEconomy = %d\n",alif,over,economy_rate1);
    // printf("Pranjal give run = %d\tOver = %d\tEconomy = %d\n",pranjal,over,economy_rate2);
    // printf("Shahil give run = %d\tOver = %d\tEconomy = %d\n",shahil,over,economy_rate3);
    // printf("Torongo give run = %d\tOver = %d\tEconomy = %d\n",torongo,over,economy_rate4);
    // printf("Debashish give run = %d\tOver = %d\tEconomy = %d\n",debashis,over,economy_rate5);
    // printf("Rakib give run = %d\tOver = %d\tEconomy = %d\n",rakib,over,economy_rate6);
    // printf("\t\t\tTotal = %d\n",total_over);

    float x = 23.245f;
    float y = 15.124f;
    float z = x+y;
    float p = x-y;
    float q =  x*y;
    float r = x/y;
    

    printf("addition of = %f +  %f = %f\n",x,y,z);
    printf("Subtraction of = %f -  %f = %f\n",x,y,p);
    printf("Multiplication of = %f *  %f = %f\n",x,y,q);
    printf("Division of = %f /  %f = %f\n",x,y,r);
    return 0;
}
